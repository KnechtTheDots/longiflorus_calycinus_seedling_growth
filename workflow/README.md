This directory contains two sub-directories and a file. The sub-directory **envs** contains information to recreate the environment
used for the analysis. The sub-directory **scripts** contains all the scripts used for the analysis. The file *Snakefile* is a 
python script that is used by snakemake to execute the workflow.